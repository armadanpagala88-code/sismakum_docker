# Nginx Configuration untuk SISMAKUM
# Copy file ini ke /etc/nginx/sites-available/sismakum
# Lalu aktifkan dengan: sudo ln -s /etc/nginx/sites-available/sismakum /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    root /var/www/sismakum/backend/public;
    index index.php index.html;

    # Log files
    access_log /var/log/nginx/sismakum_access.log;
    error_log /var/log/nginx/sismakum_error.log;

    # Increase upload size limit
    client_max_body_size 50M;

    # API Routes - Laravel
    location /api {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Laravel public files (PHP)
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
        fastcgi_read_timeout 300;
    }

    # Vue.js SPA - handle all routes (must be after PHP location)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Static files caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Deny access to sensitive files
    location ~ /\.(env|git|svn) {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# HTTPS Configuration (setelah setup SSL)
# Uncomment dan sesuaikan setelah mendapatkan SSL certificate

# server {
#     listen 443 ssl http2;
#     server_name your-domain.com www.your-domain.com;
#     root /var/www/sismakum/backend/public;
#     index index.php index.html;
#
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # ... (sama seperti konfigurasi di atas)
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name your-domain.com www.your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
